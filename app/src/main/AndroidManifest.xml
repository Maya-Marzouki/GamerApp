<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GamerApp">

        <!-- Splash = premier écran -->
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.GamerApp">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Login -->
        <activity android:name=".LoginActivity" android:exported="true" />

        <!-- Sign Up -->
        <activity android:name=".SignUpActivity" android:exported="true" />

        <!-- Forgot Password -->
        <activity android:name=".ForgotPasswordActivity" android:exported="true" />

        <!-- OTP Validation -->
        <activity android:name=".OTPValidationActivity" android:exported="true" />

        <!-- Reset Password -->
        <activity android:name=".ResetPasswordActivity" android:exported="true" />

        <!-- Home -->
        <activity android:name=".HomeActivity" android:exported="true" />

    </application>

</manifest>